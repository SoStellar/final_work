apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.ong-test.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  type: {{ .Values.ong-test.service.type }}
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80