apiVersion: v1
kind: Service
metadata:
  name: {{ include "ong-helm.fullname" . }}
  labels:
    {{- include "ong-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ong-helm.selectorLabels" . | nindent 4 }}
